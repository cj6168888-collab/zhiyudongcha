# 安全政策

## 安全原则

生语助手涉及长期记忆、文件、照片、邮件、声纹、App 操作、远程控制和蜂群协作。任何安全问题都可能影响主人的隐私、财产、关系和工作。

最高原则：

```text
主人长期利益
> 主人当前命令
> 任务完成速度
> 模型自我表现
> 资源消耗
```

## 报告范围

请优先报告：

- 越权访问。
- 敏感数据误外传。
- 高风险确认绕过。
- 记忆删除后仍被召回。
- 蜂群误共享个人资料。
- 密钥、token、验证码泄露。
- 任意文件读取或删除。
- 远程控制越界。
- 审计日志缺失或可篡改。

## 处理原则

发现疑似漏洞时：

1. 不公开传播漏洞细节。
2. 保留复现路径、requestId、日志摘要和影响范围。
3. 立即停止相关高风险自动化。
4. 修复后补充测试和审计。
5. 必要时轮换密钥和通知受影响主人。

## 密钥和敏感数据

禁止提交：

- `.env`
- `.env.production`
- provider key
- JWT secret
- session secret
- database password
- Redis password
- 声纹样本
- 私人邮件、照片、合同、聊天原文

模板只能放入 `.env.example` 和 `.env.production.example`。

## 高风险动作

以下动作必须确认并审计：

- 发送消息。
- 上传文件。
- 共享资料。
- 删除。
- 付款。
- 修改账号。
- 创建密钥。
- 外传 S2-S4 数据。
- 蜂群广播和节点权限变更。


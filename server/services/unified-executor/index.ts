/**
 * 统一执行器 - 导出索引
 */

export { UnifiedExecutor, unifiedExecutor, default } from './UnifiedExecutor';
export type {
  UnifiedDeviceType,
  UnifiedActionType,
  UnifiedActionParams,
  UnifiedResult,
  DeviceCapabilities,
} from './UnifiedExecutor';

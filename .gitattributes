# Drizzle hashes migration files from the working tree, so keep them byte-stable
# across Windows and Unix checkouts.
.gitattributes text eol=lf
migrations/*.sql text eol=lf
migrations/meta/*.json text eol=lf
